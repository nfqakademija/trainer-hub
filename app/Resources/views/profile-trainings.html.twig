
{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            {% for training in trainings %}
                <h1>{{ training.title }}</h1>
            {% endfor %}
        </div>
    </div>
</div>
    {% endblock %}
