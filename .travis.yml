language: php
php:
    - '7.1'
before_script: 
    - cp app/config/parameters.yml.dist app/config/parameters.yml
    - composer install
services:
    - mysql
script:
    - vendor/bin/phpcs -p --standard=Symfony2 --extensions=php ./src
